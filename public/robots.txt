User-agent: *
Allow: /

# Disallow nothing — all tool pages are public and should be indexed
Disallow:

# Sitemap location (dynamically updated at build time)
# The build process replaces this with the actual deployment domain
Sitemap: https://studenttoolhub.com/sitemap.xml

